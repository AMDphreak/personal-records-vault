/**
 * Bootstrap entry for the Personal Records Vault.
 * Domain logic will move into `vault.*` modules as the tree grows.
 */
module app;

import std.stdio;

void main()
{
    writeln("personal-records-vault: bootstrap build OK.");
    writeln("Read README.adoc and docs/explanation/vision-and-constraints.adoc for scope and boundaries.");
}
