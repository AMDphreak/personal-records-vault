ifndef::env-github[:icons: font]
:toc: preamble
:toclevels: 2
:sectanchors:

image:https://img.shields.io/github/contributors/AMDphreak/personal-records-vault.svg?style=for-the-badge[Contributors]
image:https://img.shields.io/github/forks/AMDphreak/personal-records-vault.svg?style=for-the-badge[Forks]
image:https://img.shields.io/github/stars/AMDphreak/personal-records-vault.svg?style=for-the-badge[Stars]
image:https://img.shields.io/github/issues/AMDphreak/personal-records-vault.svg?style=for-the-badge[Issues]
image:https://img.shields.io/github/license/AMDphreak/personal-records-vault.svg?style=for-the-badge[License]

= Personal Records Vault

Local-first, user-held structured personal records with hooks for verifiable credentials, multi-modal indexing, and selective disclosure toward providers and employers.

link:https://github.com/AMDphreak/personal-records-vault/issues[Report Bug] ·
link:https://github.com/AMDphreak/personal-records-vault/issues[Request Feature]

== About The Project

The primary application shell is an **Electron + SolidJS** desktop app (cross-platform installers). A **WebExtension** companion targets Chromium (Chrome, Edge) and Firefox (MV3). The **marketing and web vault shell** lives in `apps/site` (SolidStart 1 static site for Netlify). An optional **D** CLI remains under `native/cli` for experiments, not the main UI.

WARNING: This repository is experimental research software. It is not a medical device, not a HIPAA compliance product, and not legal advice about health records in your jurisdiction.

=== Built With

* Electron + SolidJS (desktop)
* SolidStart (marketing / web vault)
* MV3 WebExtension
* TypeScript SDK (`@prv/sdk`)

== Changelog

See link:CHANGELOG.adoc[CHANGELOG.adoc] and the link:changelog-details/[changelog-details] folder for dated design notes.

== Documentation

* link:docs/plan/index.adoc[Implementation plans (index) — unimplemented and partial areas]
* link:docs/explanation/vision-and-constraints.adoc[Vision, constraints, and open questions]
* link:docs/explanation/provider-sdk.adoc[Provider SDK, OpenAPI contract, and language adapters]
* link:apps/site/README.adoc[Marketing / web vault site (`@prv/site`)]

== Provider SDK (care software developers)

* **OpenAPI:** link:spec/openapi/prv-local.openapi.yaml[`spec/openapi/prv-local.openapi.yaml`] — generate Java, C#, Python, Go, and other clients from this file.
* **JSON Schema:** link:spec/json-schema/exchange-bundle.schema.json[`spec/json-schema/exchange-bundle.schema.json`] — bundle payload validation.
* **TypeScript reference:** link:packages/sdk/README.adoc[`packages/sdk` (`@prv/sdk`)] — `createVaultClient({ baseUrl })` for loopback HTTP calls.

Build the SDK with `pnpm --filter @prv/sdk build` (also runs as part of `pnpm build` at the repo root).

== Getting Started

=== Prerequisites

* https://nodejs.org/[Node.js] 22.x (see also `packageManager` in link:package.json[package.json])
* https://pnpm.io/[pnpm] 9.x (Corepack can install the pinned version)

=== Desktop app (Electron + Solid)

[source,powershell]
----
pnpm install
pnpm dev
----

Production bundle (Vite output under `apps/desktop/out`):

[source,powershell]
----
pnpm build
----

Installers (NSIS on Windows, DMG on macOS, AppImage on Linux) via https://www.electron.build/[electron-builder]:

[source,powershell]
----
pnpm dist
----

Artifacts land in `apps/desktop/release/`.

=== Marketing site (SolidStart static, Netlify)

The static site is built with **SolidStart 1** (`vinxi build`) and emits HTML to `apps/site/.output/public`.

[source,powershell]
----
pnpm dev:site
pnpm build:site
----

Netlify picks up link:netlify.toml[`netlify.toml`] at the repository root: install dependencies, run `pnpm --filter @prv/site build`, and publish `apps/site/.output/public`.

In the Netlify UI, connect the GitHub repository, leave the base directory empty (repo root), and confirm the build image uses **Node 22** and **pnpm** (the config sets `NODE_VERSION`).

The `/login` route stores a **session marker** in `sessionStorage` only. The `/app` route uses the **File System Access API** to list a folder you select (for example on a USB drive); nothing is uploaded to Netlify.

=== Browser extension

Build output is written to `apps/extension/dist/`.

[source,powershell]
----
pnpm build:extension
----

Load unpacked:

* *Chrome / Edge:* `chrome://extensions` or `edge://extensions` → Developer mode → *Load unpacked* → select `apps/extension/dist`.
* *Firefox:* `about:debugging#/runtime/this-firefox` → *Load Temporary Add-on* → pick `apps/extension/dist/manifest.json`.

=== Optional D CLI

[source,powershell]
----
Set-Location native/cli
dub build
dub run
----

== CI and releases

* link:.github/workflows/ci.yml[CI] builds the desktop, extension, SDK, and static site on Ubuntu.
* link:.github/workflows/release-desktop.yml[Release desktop] runs on `workflow_dispatch` and on tags matching `v*` to produce per-OS installers and upload GitHub Actions artifacts.

Codesigning and macOS notarization are not configured yet; expect unsigned macOS binaries until you add certificates and entitlements.

== Repository layout

[cols="1,2"]
|===
| Path | Purpose

| `apps/desktop/` | Electron main/preload/renderer (SolidJS; plain controls for now)
| `apps/site/` | SolidStart 1 **static** marketing site + web vault shell (`@prv/site`, Netlify publish dir `.output/public`)
| `apps/extension/` | MV3 WebExtension popup scaffold
| `netlify.toml` | Netlify build and publish configuration for the static site
| `spec/openapi/` | Local Provider **OpenAPI** contract for multi-language client generation
| `spec/json-schema/` | Payload **JSON Schema** mirrors for validators
| `packages/sdk/` | TypeScript **reference client** (`@prv/sdk`)
| `docs/explanation/` | Architecture and product reasoning (AsciiDoc)
| `docs/plan/` | Phased implementation plans for unfinished or partial subsystems (start at `index.adoc`)
| `changelog-details/` | Dated narrative notes linked from the changelog
|===

== Contributing

Fork the repo, create a feature branch, and open a pull request. See open issues for known gaps and plans under `docs/plan/`.

== License

See the repository `LICENSE` / `LICENSE.txt` file.

== Contact

Ryan Johnson — https://twitter.com/amdphreak[@amdphreak]

* Project: https://github.com/AMDphreak/personal-records-vault
* Site: https://ryanjohnson.dev
